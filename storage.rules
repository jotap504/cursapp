rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Course files — only authenticated admins can write, anyone can read
    match /courses/{courseId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
